{
  "name": "web-header",
  "version": "1.0.0",
  "description": "Bootstrap, template and assets to display Istex galaxy header.",
  "private": true,
  "scripts": {
    "prettier": "prettier --write src/js/*.js public/css/*.css",
    "webpack": "webpack --config webpack.config.js",
    "build": "npm run prettier && npm run webpack"
  },
  "dependencies": {
    "nanoajax": "^0.4.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/istex/istex-web-header"
  },
  "keywords": [
    "Istex",
    "header"
  ],
  "author": "Istex API",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.12.1",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.14"
  }
}
