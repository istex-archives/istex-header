{
  "name": "web-header",
  "version": "1.0.0",
  "description": "Bootstrap, template and assets to display Istex galaxy header.",
  "main": "main.js",
  "scripts": {
    "precommit": "lint-staged"
  },
  "dependencies": {
    "nanoajax": "^0.4.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/istex/istex-web-header"
  },
  "keywords": [
    "Istex",
    "header"
  ],
  "author": "Istex API",
  "license": "MIT",
  "lint-staged": {
    "*.{js,less,html}": [
      "prettier --write /js/*.js /views/*.html",
      "git add"
    ]
  },
  "devDependencies": {
    "prettier": "^1.10.2"
  }
}
